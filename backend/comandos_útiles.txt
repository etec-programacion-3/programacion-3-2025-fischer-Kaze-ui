#activar venv, se hace siempre al incio: source venv/bin/activate
#desactivar venv: deactivatesudo service postgresql start
#iniciar PostgreSQL, se hace siempre que inicio: 
#ver estado de postgresql: sudo service postgresql status
#entrar a la consola posgres: sudo -u postgres psql
#postgres contraseña: 12345*
#ejectuar archivo como parte de un paquete: python -m backend.main
#Iniciar el backend enlazando a todas las interfaces: (desde la raíz del proyecto)
#source venv/bin/activate
#cd backend
#uvicorn main:app --host 0.0.0.0 --port 8000
#MÁS CONFIABLE-podrías tener que usar el siguiente comando para encender el backend, pero no logro diferenciar cuando requiero este comando y cuandoi el otro: python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
#apagar backend: CTRL+C
#activar entorno virtual WSL Ubuntu: source venv-ubuntu/bin/activate
#Base: http://localhost:8000
#Docs: http://localhost:8000/docs (Swagger automático de FastAPI)
#instalar dotenv: pip install python-dotenv alembic
#WSL ubuntu sudo password, es: ubuntu